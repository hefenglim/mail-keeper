# 認證快取（含有效憑證，切勿提交）
token_cache.bin

# 使用者本機設定（client_id / email；使用者專屬，不提交）
config.json

# 釋出暫存（內含建置產物與 token 快取，不提交）
released/

# 含 client_id 的個人記事，不提交
*Client ID*.txt
*識別碼*.txt

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.pytest_cache/
*.tmp

# 全端 E2E 產生的 trace log（由 tests/e2e/e2e_bulk_30000.py 重新產生，不提交）
e2e-trace-logs/

# 測試 / 覆蓋率 / 突變測試產物
.coverage
.coverage.*
htmlcov/
.mutmut-cache
.mypy_cache/

# 建置時烙印的 build 時間戳（由 scripts/stamp-build.py 產生，不提交）
src/mailkeeper/_buildinfo.py

# CPython imaplib 參考資料（原始碼 + 文件），不提交
imaplib/
